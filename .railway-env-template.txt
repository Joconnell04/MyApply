# Railway Environment Variables Template
# Copy these to your Railway service settings

# === REQUIRED ===

# Database (set automatically by Railway Postgres plugin)
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@postgres.railway.internal:5432/railway

# Security & Sessions
SECRET_KEY=[GENERATE_WITH_python_secrets.token_urlsafe(32)]
SESSION_COOKIE_NAME=app_session
SESSION_SECURE=true
SESSION_SAMESITE=lax

# Environment
ENV=production
PORT=8000

# OpenAI
OPENAI_API_KEY=[YOUR_OPENAI_API_KEY]
LLM_MODEL=gpt-4o-mini
COVER_LETTER_MODEL=gpt-4o

# ChatKit (if using)
CHATKIT_DOMAIN=production.up.railway.app
CHATKIT_DOMAIN_PUBLIC_KEY=[YOUR_CHATKIT_DOMAIN_PUBLIC_KEY]

# MyApply API Key
MYAPPLY_API_KEY=[GENERATE_NEW_VALUE]

# === OPTIONAL ===

# Mapbox (for map features)
MAPBOX_PUBLIC_TOKEN=[YOUR_MAPBOX_PUBLIC_TOKEN]
MAPBOX_ACCESS_TOKEN=[YOUR_MAPBOX_ACCESS_TOKEN]

# CORS (Railway domain will be added automatically)
ALLOWED_ORIGINS=https://myapply-production.up.railway.app

# Admin User (for initial setup)
ADMIN_EMAIL=[YOUR_ADMIN_EMAIL]
ADMIN_PASSWORD=[SET_IN_RAILWAY_SECURELY]
